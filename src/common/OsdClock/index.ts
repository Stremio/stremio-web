import { OsdClockProvider, useOsdClock } from './OsdClock';
import { formatClockTime } from './helpers';
import type { ClockFormat, ClockPosition } from './helpers';

export {
    OsdClockProvider,
    useOsdClock,
    formatClockTime,
};
export type { ClockFormat, ClockPosition };
